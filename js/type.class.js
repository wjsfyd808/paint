export default class Type
{
    constructor(color, fillStyle,strokeStyle,lineWidth,brushSize )
    {
        this.color = color;
        this.fillStyle = fillStyle;
        this.strokeStyle = strokeStyle;
        this.lineWidth = lineWidth;
        this.brushSize = brushSize;
    }
}